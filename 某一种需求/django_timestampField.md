```
需求:
展示 格式化时间
存储 无所谓
搜索 支持毫秒搜索
在model中定义field,然后在序列化器和搜索器中可以通过修改继承方法获取不修改直接使用,而不是需要再次定义对应的field属性
保存进来是时间戳,展示出去是格式化的时间
```



```
# 实现一
数据库存储bigint格式,只需在返回上将时间戳转化为时间格式
需要添加auto_add_now auto_add 等属性

# 实现二
复用django.DatetimeField,需要修改搜索方法(搜索器),修改格式化方法(模型类),需要修改保存的方法(序列化器)
```

```

```

```
auto_now_add 代表创建时add一个时间,add只有一次
auto_now 代表每次都会将当前时间更新
```

